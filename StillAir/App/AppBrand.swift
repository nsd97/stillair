import Foundation

/// User-facing product identity. StillAir is a read-only thermal monitor for
/// fanless Macs and can sit beside fan-control apps (ChillMac / Fan Sooner).
enum AppBrand {
    static let displayName = "StillAir"
    static let applicationSupportName = "StillAir"
}
